apiVersion: v1
kind: Service
metadata:
  name: {{ include "scale-agent-demo.fullname" . }}
  labels:
    {{- include "scale-agent-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scale-agent-demo.selectorLabels" . | nindent 4 }}
