
get_last_pod() {
    local node="10.128.0.124"
	local content=(`kubectl get pod -A -o wide | grep 10.128.0.124 | tail -n 1`)
	ns=${content[0]}
    pod=${content[1]}
}

echo_proxy() {
	echo "[`date '+%F %T'`] $@"
}

get_last_pod
export POD_NAMESPACE="$ns"
export HOSTNAME="$pod"
export ROOTFS="/"
